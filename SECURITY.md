# Отчёт о тестировании безопасности


## Сводные результаты

| Блок тестов | Всего | ✅ PASS | ❌ FAIL |
|-------------|-------|---------|---------|
| 1. Базовая работа | 3 | 3 | 0 |
| 2. Несанкционированный доступ | 4 | 4 | 0 |
| 3. SQL-инъекции | 6 | 6 | 0 |
| 4. Валидация полей | 5 | 5 | 0 |
| 5. Неподдерживаемые методы | 3 | 3 | 0 |
| **ИТОГО** | **21** | **21** | **0** |

✅ Критических уязвимостей не обнаружено. Все 21 тест пройден успешно.

---

## Блок 1 — Базовая работа

| № | Тест | Ожидаемый результат | Статус |
|---|------|---------------------|--------|
| 1.1 | Создание устройства | 201 + device_id | ✅ PASS |
| 1.2 | Получение погоды по device_id=1 | 200 + JSON с погодой | ✅ PASS |
| 1.3 | Повторный запрос — кэш | 200 + from_cache: true | ✅ PASS |

---

## Блок 2 — Несанкционированный доступ

| № | Тест | Ожидаемый результат | Статус |
|---|------|---------------------|--------|
| 2.1 | Запрос без device_id | 401 Unauthorized | ✅ PASS |
| 2.2 | Несуществующий device_id=999999 | 404 Not Found | ✅ PASS |
| 2.3 | Отрицательный device_id=-1 | 404 Not Found | ✅ PASS |
| 2.4 | Нулевой device_id=0 | 404 Not Found | ✅ PASS |

**Вывод:** запрос без `device_id` возвращает 401 — анонимный доступ к данным исключён. Несуществующие идентификаторы возвращают 404 без раскрытия деталей о структуре БД.

---

## Блок 3 — SQL-инъекции

| № | Тест | Payload | Результат | Статус |
|---|------|---------|-----------|--------|
| 3.1 | DROP TABLE в поле name | `'; DROP TABLE user; --` | 201, таблица цела | ✅ PASS |
| 3.2 | OR 1=1 в поле city | `' OR '1'='1` | 201, логика не нарушена | ✅ PASS |
| 3.3 | OR 1=1 в device_id | `?device_id=1 OR 1=1` | 400, отклонено до БД | ✅ PASS |
| 3.4 | Time-based инъекция | `?device_id=1;SELECT pg_sleep(5)--` | 400, ответ за 0с | ✅ PASS |
| 3.5 | Сервер не завис | Замер времени ответа | Ответ мгновенный (<1с) | ✅ PASS |
| 3.6 | БД жива после инъекций | POST /create_user после тестов | 201, БД работает | ✅ PASS |

**Почему инъекции невозможны:** все запросы используют параметризацию через pgx — значения передаются отдельно от SQL по протоколу PostgreSQL wire protocol:
```go
tx.QueryRow(ctx, `INSERT INTO "user"(name) VALUES ($1) RETURNING id`, name)
q.runner.QueryRow(ctx, `SELECT city FROM "device" WHERE id = $1`, deviceID)
```

---

## Блок 4 — Валидация входных данных

| № | Тест | Данные | Ожидаемый результат | Статус |
|---|------|--------|---------------------|--------|
| 4.1 | Пустые поля name и city | `{name: '', city: ''}` | 400 + сообщение об ошибке | ✅ PASS |
| 4.2 | Имя длиннее 100 символов | name = 101 символ | 400 + under 100 characters | ✅ PASS |
| 4.3 | Город длиннее 100 символов | city = 101 символ | 400 + under 100 characters | ✅ PASS |
| 4.4 | Сломанный JSON | `{name: Ivan}` без кавычек | 400 + parse error | ✅ PASS |
| 4.5 | Неверный Content-Type | Content-Type: text/plain | 400 + parse error | ✅ PASS |

---

## Блок 5 — Неподдерживаемые HTTP-методы

| № | Тест | Запрос | Ожидаемый результат | Статус |
|---|------|--------|---------------------|--------|
| 5.1 | DELETE на /api/weather | DELETE /api/weather | 405 Method Not Allowed | ✅ PASS |
| 5.2 | PUT на /create_user | PUT /create_user | 405 Method Not Allowed | ✅ PASS |
| 5.3 | Несуществующий эндпоинт | GET /api/admin | 404 Not Found | ✅ PASS |

---

## Права доступа к базе данных

| Замечание | До | После | Статус |
|-----------|-----|-------|--------|
| Подключение под суперпользователем postgres | postgres (superuser) | weather_app (минимальные права) | ✅ Исправлено |

Создан отдельный пользователь `weather_app` с минимальными привилегиями через `init.sql`:
```sql
CREATE USER weather_app WITH PASSWORD 'weather_app_password';
GRANT CONNECT ON DATABASE postgres TO weather_app;
GRANT USAGE, CREATE ON SCHEMA public TO weather_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO weather_app;
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO weather_app;
```

---

## Выводы

### Положительные результаты
- SQL-инъекции полностью исключены — все запросы параметризованы через pgx
- Запрос без `device_id` возвращает 401 — анонимный доступ закрыт
- Ошибки БД не раскрываются клиенту — логируются только на сервере
- Валидация входных данных реализована (пустые поля, длина, тип)
- Некорректные HTTP-методы отклоняются с кодом 405
- Приложение подключается к БД с минимальными привилегиями
